---
interface Props {
  width?: 'narrow' | 'default' | 'wide';
}

const { width = 'default' } = Astro.props;

const widthClass = {
  narrow: 'max-w-3xl',
  default: 'max-w-5xl',
  wide: 'max-w-7xl',
}[width];
---

<div class={`${widthClass} mx-auto px-6 md:px-8`}>
  <slot />
</div>
