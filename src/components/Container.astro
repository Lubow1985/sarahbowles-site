---
interface Props {
  width?: 'narrow' | 'default' | 'wide';
}

const { width = 'default' } = Astro.props;

const widthStyle = {
  narrow: 'max-width: 68ch',
  default: '',
  wide:    'max-width: 90rem',
}[width];
---

<div class="container" style={widthStyle || undefined}>
  <slot />
</div>
